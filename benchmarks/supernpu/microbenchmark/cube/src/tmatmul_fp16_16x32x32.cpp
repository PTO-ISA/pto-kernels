#include "cube_bench.hpp"
// auto-generated by gen_cases.py
// TMATMUL (matmul) fp16 16x32x32
int main() {
    constexpr int M = 16, N = 32, K = 32;
    __half a[M*K], b[K*N], c[M*N];
    float bias[1*N];
    fill_seq(a, M*K); fill_seq(b, K*N); fill_seq(bias, N);
    zero(c, M*N);
    BENCHSTART;
    bench_matmul<__half,M,N,K>(c,a,b);
    BENCHEND;
    return 0;
}
