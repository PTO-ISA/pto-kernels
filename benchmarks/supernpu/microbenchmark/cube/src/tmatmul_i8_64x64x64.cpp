#include "cube_bench.hpp"
// auto-generated by gen_cases.py
// TMATMUL (matmul) i8 64x64x64
int main() {
    constexpr int M = 64, N = 64, K = 64;
    int8_t a[M*K], b[K*N], bias[1*N], as[M*K], bs[K*N], c[M*N];
    fill_seq(a, M*K); fill_seq(b, K*N); fill_seq(bias, N);
    fill_const(as, M*K, (int8_t)1); fill_const(bs, K*N, (int8_t)1); zero(c, M*N);
    BENCHSTART;
    bench_matmul<int8_t,M,N,K>(c,a,b);
    BENCHEND;
    return 0;
}
