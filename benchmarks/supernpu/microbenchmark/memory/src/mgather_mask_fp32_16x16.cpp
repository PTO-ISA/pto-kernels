#include "memory_bench.hpp"
// auto-generated by gen_cases.py
// MGATHER_MASK (gather_mask) fp32 16x16
int main() {
    constexpr int M = 16, N = 16;
    float a[M*N], c[M*N], idx[M*N], mask[M*N];
    fill_seq(a, M*N); fill_idx(idx, M*N); fill_const(mask, M*N, (float)1); zero(c, M*N);
    BENCHSTART;
    bench_gather_mask<float,M,N>(c,a,idx,mask);
    BENCHEND;
    return 0;
}
