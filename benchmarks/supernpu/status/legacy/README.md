# Legacy SuperNPUBench material

Everything below this directory is non-normative imported history. It is kept
only for provenance and migration reference and must not be used as the current
ISA, assembly, compiler, or benchmark contract.

Current agents and build scripts start at `benchmarks/supernpu/README.md` and
must not index this directory unless a task explicitly requests historical
analysis.
