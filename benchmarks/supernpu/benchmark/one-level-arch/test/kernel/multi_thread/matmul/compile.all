#!/bin/bash
# Multi-thread GMMA matmul operator.

: "${COMPILER_DIR:?Set COMPILER_DIR to the Linx compiler bin directory}"

make TESTCASE=matmul \
    COMPILER_DIR="$COMPILER_DIR" \
    B=1 M=256 N=256 K=256 \
    tM=32 tN=32 tK=32
