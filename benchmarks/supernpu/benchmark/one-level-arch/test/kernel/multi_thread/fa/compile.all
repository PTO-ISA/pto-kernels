#!/bin/bash
# Multi-thread GMMA FlashAttention operator.

: "${COMPILER_DIR:?Set COMPILER_DIR to the Linx compiler bin directory}"

make TESTCASE=fa_2d_unroll_gmma \
    COMPILER_DIR="$COMPILER_DIR" \
    Sq=128 Skv=64 Tm=16 Tk=16
