#!/bin/bash
# Multi-thread vector-add operator.

: "${COMPILER_DIR:?Set COMPILER_DIR to the Linx compiler bin directory}"

make TESTCASE=tadd \
    COMPILER_DIR="$COMPILER_DIR" \
    TileRows=16 TileCols=16

make TESTCASE=trowsum \
    COMPILER_DIR="$COMPILER_DIR" \
    TileRows=16 TileCols=16
